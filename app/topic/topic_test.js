'use strict';

describe('myApp.topic module', function() {

  beforeEach(module('myApp.topic'));

  describe('topic controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('TopicCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});